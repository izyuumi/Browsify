name: PR Build Check

on:
  pull_request:
    branches: [main]

jobs:
  test:
    runs-on: macos-26
    steps:
      - uses: actions/checkout@v4

      - name: Test macOS app
        run: |
          xcodebuild \
            -project Browsify.xcodeproj \
            -scheme Browsify \
            -destination 'platform=macOS' \
            CODE_SIGNING_ALLOWED=NO \
            test
